apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "java-service.fullname" . }}-test-connection"
  labels:
    {{- include "java-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "java-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
